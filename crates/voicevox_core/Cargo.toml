[package]
name = "voicevox_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "core"
crate-type = ["cdylib"]

[dependencies]
derive-getters = "0.2.0"
derive-new = "0.5.9"
once_cell = "1.10.0"
thiserror = "1.0.31"

[dev-dependencies]
rstest = "0.12.0"
pretty_assertions = "1.2.1"

[build-dependencies]
cbindgen = "0.23.0"
