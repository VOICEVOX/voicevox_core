# 0.16.3

## 変更点

ここでは主要な変更を紹介します。全ての変更点については[CHANGELOG.md]をご覧ください。

### `AudioQuery`/`AccentPhrase`/`Mora`における不正な状態というものが定義され、明示的にエラーになるように

メソッド`{AudioQuery,AccentPhrase,Mora}::validate`が追加されます。`Synthesizer`の各メソッドも内部でバリデートし、早期に明示的なエラーを出すようになります。

```py
from voicevox_core import InvalidQueryError

# 例: `vowel`に子音
audio_query.accent_phrases[0].moras[0].vowel = "k"

try:
    audio_query.validate()
except InvalidQueryError as e:
    ...
```

一部の状態については警告を出すことに留めていますが、将来的にすべてエラーになる予定です。

### バグ修正

ダウンローダーにおける辞書 (`dict`)のダウンロード元であるjaist.dl.sourceforge.netが[利用できなく]なりダウンロード不可になったため、ダウンロード先を変更しました。

## 謝辞

以下の方々から貢献をいただきました。この場を借りてお礼申し上げます。

- [@siyukatu]さん（[#1219]の報告）

[CHANGELOG.md]: https://github.com/VOICEVOX/voicevox_core/blob/main/CHANGELOG.md#unreleased
[利用できなく]: https://x.com/zinchang/status/1996112944372044235
[@siyukatu]: https://github.com/siyukatu
[#1219]: https://github.com/VOICEVOX/voicevox_core/issues/1219
